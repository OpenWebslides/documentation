# Feed Items API

## Get all feed items

This endpoint retrieves all Recent Activity feed items.

**Response attributes**

Attribute       | Type | Description
--------------- | ---- | -----------
`eventType`     | Enum   | Type of event. Can be one of `topic_created`, `topic_updated`
`userName`      | String | Name of the user that was involved in the event
`topicTitle`    | String | Name of the topic that was involved in the event
`createdAt`     | Integer| Timestamp of the event. **This attribute is included in the `meta` section.**

> Request

```http
GET <%= current_page.data.endpoint %>feedItems HTTP/1.1
Accept: application/vnd.api+json, application/vnd.openwebslides+json; version=<%= current_page.data.version %>
```

> Success response

```http
HTTP/1.1 200 OK
Content-Type: application/vnd.api+json
```

```json
<%= api_response 'feed_items/get_feed_items' %>
```
